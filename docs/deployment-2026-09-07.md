# Tencent2 已部署新版 GitHub Radar

访问地址：https://github-radar.43-160-242-46.sslip.io/?lang=zh-CN

2026-09-07 已发布程序版本 `026e8f9f02fe9230e3f92ede1f7b9e53d720f8c8`。
线上使用原数据库，没有用本地预览副本覆盖数据，也没有导入本地测试项目。

## 程序和业务数据保存在数据盘

数据盘挂载点是 `/home/xingzheng/data`，对应 `/dev/vdb`。
项目目录是 `/home/xingzheng/data/github-radar`。

- 程序和源码保存在 `releases/20260907-026e8f9`。
- 数据库仍为 `github-radar.db`，导出和日常备份仍在 `exports`、`backups`。
- 发布前的数据库、程序及配置保存在 `deployment-backups/20260907-026e8f9`，该目录只允许管理员读取。
- `/opt/github-radar/github-radar` 是指向数据盘发布版本的软链接。

环境及采集配置仍保存在 `/etc/github-radar`。原 GitHub 凭据保持不变。
线上已禁用 OSS Insight，并采用新版 GitHub 搜索配置和七个顶级分类。

## 访问、采集和数据完整性已验证

完整测试、并发测试、Linux 构建和安全扫描通过。
服务由 `github-radar-web.service` 保持运行，已启用开机启动。

发布时将数据库结构从 v1 升级到 v3。升级前后均有 4,126 个项目和
39,062 条每日观测记录，数据库完整性检查通过。
原有 161 条历史主题外键异常保持不变，本次没有删除或修复这些记录。

历史项目已按新版规则补充分类，匹配了 878 个项目，更新了 1,293 项分类关联。
此操作不修改 Star 历史，人工分类保护规则继续生效。

公网的看板、项目库、项目详情、分类、采集记录、添加表单、静态资源和
健康检查均返回 HTTP 200。HTTP 会跳转 HTTPS，未授权添加请求返回 403。
Chrome 后台页面验证确认主导航只有两项，添加按钮只在项目库出现一次。

使用线上 GitHub 凭据测试了标杆项目搜索：返回 38 个候选，更新 38 个已有项目，
没有重复入库，也没有请求失败。

每日任务试跑跳过了今天已成功采集的 4,105 个项目，另有 9 个项目返回
GitHub 404，因此该批次标为部分成功。CSV、JSON 导出及 SQLite 备份均已完成。
失败记录保留在采集历史中，不会被当作零增长。

## 每日任务和访问权限保持独立

GitHub Radar 继续在北京时间每天 09:15 运行采集。
原有 09:00 飞书日报任务未改动。

现有 Nginx 域名配置与 HTTPS 证书继续使用，证书到期日为 2026-11-28，
`certbot-renew.timer` 已配置自动续期。本次没有改动其他站点的代理配置。

公开浏览不需要登录。添加关注需要独立管理密钥，保存在服务器的
`/etc/github-radar/web-management-token`，权限为管理员只读写。
密钥值不写入文档、源码或日志，也不是 GitHub API 凭据。

## 回退文件已保留

旧程序另存为 `/opt/github-radar/github-radar.previous`。
上述发布备份目录还保留升级前数据库和配置。
程序回退与数据库恢复需要分别判断，恢复数据库前应停止写入并保存当前数据。
