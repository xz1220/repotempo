# 当天新增阅读页与简读补全已上线

2026-09-08，Tencent2 已部署 `f4f0b54`。[项目库](https://github-radar.43-160-242-46.sslip.io/repositories?lang=zh-CN)默认展示北京时间当天新入库项目，按 Star 排序。

## 先读今天，再看全库

项目库提供当天新入库、全部项目、我的关注三个视图，主导航仍只有趋势看板和项目库。显式日期、排序和已有筛选链接保留原意；没有新增的日期显示真实空状态。

每张卡片有已读按钮，点击后底色变浅，可以改回未读。标记按永久仓库 ID 保存在当前浏览器和站点，刷新、翻页和同源标签页可保持；不跨设备或域名同步。

打开详情、滚动和补入简读都不会自动标记已读。存储失败会提示未保存，不伪装成功；没有 JavaScript 时仍能阅读卡片并打开详情或 GitHub。

## 今天 323 个项目全部有简读

| 范围 | 项目数 | 已有说明 | 仍待补齐 |
| --- | --- | --- | --- |
| 9 月 8 日新增 | 共 323 个项目。 | 323 个已有说明。 | 没有缺失项。 |
| 其他历史项目 | 共 4,135 个项目。 | 1,192 个已有说明。 | 2,943 个仍待补齐。 |
| 全库 | 共 4,458 个项目。 | 1,515 个已有说明。 | 2,943 个仍待补齐。 |

本轮新写入 1,505 份说明，已有 10 份完全保留。新写入内容包括今天 323 份 README 简读，以及 1,182 份历史研究说明。

历史来源原有 1,183 个可用候选，其中 MarkuprPlus 与今天的项目重合，最终采用今天的新阅读结果。回退模板、低证据和待核验旧卡没有冒充已完成简读。

今日简读包含 221 份完整 README 阅读、102 份 README 节选阅读，均保存来源链接和内容 SHA。独立助手抽查 14 个项目，与对应资料逐项核对，未发现必须修正的问题；没有运行项目，也没有独立核验作者的性能声明。

历史研究缺少可靠的原生成时间，页面明确标为“原解读时间未记录”。导入时间仅作记录，不作为原始研究日期展示。

## 数据和交互检查通过

- `make ci` 通过，包括 Go 全量测试、32 项 JavaScript 测试、并发检查和 Linux 构建。
- [GitHub CI](https://github.com/xz1220/repotempo/actions/runs/34195454039) 全部通过，包含依赖漏洞扫描。
- 独立测试库验证了批量首次导入、重复导入只跳过，以及 323 个当日项目全部有说明。
- 本地后台浏览器验证了默认日期、全部视图切换、单卡标读与撤销、刷新保留、详情阅读和 375px 手机布局。
- 公网项目页返回 20 张卡片、20 份实际简读、20 个标读按钮，没有“尚未解读”占位；新旧域名及脚本资源返回 200。

线上自动浏览器连接仍出现超时，因此不把线上截图验收列为通过。公网检查使用独立 HTTP 响应核验，浏览器视觉与交互检查使用真实数据库副本。

## 顺带修复了两个连接安全问题

远端测试暴露出 SQLite 连接重建后丢失保护设置，以及取消迁移时后台回滚与设置恢复竞跑的问题。现在每个新连接都会配置保护，迁移也会同步完成回滚后再恢复设置。

取消与强制换连接连续 40 轮、并发检查 5 轮均通过，并实际验证了非法关联写入会被拒绝。没有放宽原测试的取消期限。

线上原有项目、43,517 条每日观测及已有说明逐行对比均无变化，数据库版本仍为 4。原有 155 条历史外键检查记录数量未变，本轮没有删除或清理它们。

发布目录为 `/home/xingzheng/data/github-radar/releases/20260908-daily-reading`。部署前备份位于同一数据盘的 `deployment-backups/20260908-daily-reading`，仅管理员可读；旧程序保留，可切回。

## 尚未启用的部分

余下 2,943 个历史项目没有在本轮继续批量生成。每日自动生成简读也尚未接入定时任务，现有 Star 采集计划保持不变。

本轮提供了可复用的离线阅读工具和 `analysis import-batch`。它们按需执行，批量导入只填空、保留已有说明，任何校验或写入错误都会整批回滚。
