# RepoTempo 已更名并接入 GitHub Trending

新站点：[RepoTempo](https://repotempo.43-160-242-46.sslip.io/?lang=zh-CN)。
代码仓库更名为 `xz1220/repotempo`，仓库 ID 和原有私有可见性保持不变。
旧域名继续服务相同应用，旧命令、环境变量和数据目录继续兼容。

2026-09-07 发布的程序版本为 `dd9da567d8c811701163955f5faa9ecc685c2594`。
程序位于数据盘的 `github-radar/releases/20260907-dd9da56-repotempo`；
发布前的数据与配置位于同目录下的 `deployment-backups/20260907-dd9da56-repotempo`。

## 首批任务已验证主次关系

GitHub Trending 是主要发现源，Search 补充主题、新项目和成熟项目覆盖。
所有入库项目继续通过 GitHub 仓库 API 保存每日绝对 Star，不因离榜停止跟踪。

线上首次读取日榜 14 条、周榜 19 条、月榜 23 条；去重并通过 API 核验后有
52 个不同项目，其中新增 9 个、更新已有项目 43 个，Trending 阶段无失败。
9 个新项目均已保存首条真实 Star 快照，没有用榜单增量代替绝对 Star。

任务完成后数据库包含 4,135 个项目、39,071 条观测。今天已有成功观测的
4,105 个项目被跳过，原有 9 个不可访问项目仍返回 404，因此整批标为部分成功。
CSV、JSON 导出和 SQLite 备份均已完成。

## 兼容与验收结果

数据库结构升级为 v4，迁移前后原有快照逐行比较一致；分类关联及既有
161 条历史外键异常保持原样。数据库完整性检查通过，没有清理用户历史数据。

完整测试、竞态检查、Linux 构建及依赖安全检查通过。新增测试覆盖去重、
来源并集、改名历史、语义分类、部分失败、离榜后持续采集和 API 拒绝后的整批停止。

桌面和 375 像素宽度页面已通过 Chrome 后台验证，没有抢占前台。
主导航仍只有趋势看板和项目库，添加关注只保留一个入口。
新旧域名的健康检查均返回 200，未授权的公开写入仍返回 403。

新域名证书有效期至 2026-12-06，并由现有证书续期任务维护。
每日采集仍为北京时间 09:15；飞书日报任务、管理密钥和服务器权限均未改变。

Kimi 因五小时额度用尽未参与本次复核；命令行复核工具也未成功执行。
本次采用独立子任务的只读代码复核、自动化测试和线上真实采集交叉验证。
